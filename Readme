主要部分
1.用Pdf的規定作出輸入IP的頁面以及輸入問題還有結果頁面以及相關基本功能
2.輸入問題部分有加clean按鈕，如果按下可清空那兩個可輸入的地方
3.另外再輸入數字時有加入一按進去即只顯示數字鍵盤，增加方便性
4.若為除法第二個數字如果輸入零則不會進入下一頁（防呆）
5.結果頁面有return 跟 reconnect ，return就與pdf規定相同，跳回計算的地方，reconnect則可跳回輸入ip的地方重新輸入

遇到困難
return鍵的製作
換到該頁面後需要多設一個control signal 來判斷是否需要進入輸入IP的頁面
